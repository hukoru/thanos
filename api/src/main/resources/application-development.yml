spring:
  devtools:
    livereload:
      enabled: true
  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
    port: 8080

logging:
  path: /tmp/log
  level:
    org:
      hibernate:
        type: trace