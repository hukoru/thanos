language: java
jdk:
- openjdk8

install: true

script: gradle :api:build

before_install:
- gradle :api:clean

branches:
  only:
  - master

after_success:
- gradle :api:clean cobertura:cobertura coveralls:report